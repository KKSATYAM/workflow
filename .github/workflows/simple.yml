name: Shell Commands

on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
          - name: Echo a String
            run: |
                echo "Helllo"
                echo "My First Workflow"
          - name: Check Versions
            run: |
                npm -v
                git --version

